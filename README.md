A package of php snowflake library
=================================


# Installation

The method of installing this package is with
[Composer](https://getcomposer.org/) by running the following from your project
root:

    $ composer require "sunny-daisy/snowflake"
